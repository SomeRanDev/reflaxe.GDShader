package gdscompiler.config;

enum abstract Define(String) from String to String {
	/**
		-D gdshader.template_define

		Keeping this here to copy/replace from once I actually have real defines.
	**/
	var TemplateDefine = "gdshader.template_define";
}
