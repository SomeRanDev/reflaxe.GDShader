# DevEnv.hxml
# Makes auto-completion work in VS code while developing this project.
# Also checks null-safety when executed (`haxe DevEnv.hxml`).
-D reflaxe_runtime
-D gdshader_runtime
--macro nullSafety("gdscompiler")
-lib reflaxe
-cp src
gdscompiler